context:
  name: rocsolver
  version: "7.2.4"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  - url: "https://github.com/ROCm/rocm-libraries/releases/download/rocm-${{ version }}/rocsolver.tar.gz"
    sha256: 174ddf22656950984dea1768d9232df5278a81c747a6eb272670e80cfac262a7

build:
  number: 0
  skip: 
    - not linux
    - hip_compiler_version in (None, "None")

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - ${{ compiler('cxx') }}
    - ${{ compiler('hip') }}
    - cmake
    - pkg-config
    - ninja
  host:
    - rocm-core ${{ version }}.*
    - rocm-cmake
    - rocblas
    - rocprim
  run_exports:
    - ${{ pin_subpackage(name, upper_bound='x.x') }}

tests:
  # needs hip-devel
  #- requirements:
  #    run:
  #      - cmake-package-check
  #      - ${{ compiler('c') }}
  #      - ${{ compiler('cxx') }}
  #  script:
  #    - cmake-package-check ${{ name }}
  - package_contents:
      include:
        - ${{ name }}/${{ name }}.h
      lib:
        - ${{ name }}

about:
  homepage: https://rocm.docs.amd.com/projects/rocSOLVER
  license: MIT
  license_family: MIT
  license_file: LICENSE.md
  summary: "AMD ROCm SOLVER library."

extra:
  recipe-maintainers:
    - traversaro